import quantile from '../../quantile.ts';

const beta = quantile.distributions.beta;

/// Test
export default function test_beta() {
    // // Tests for the mean and variance of the beta distribution.
    // console.assert(beta(0.4).mean === 0, 'Etc.');
    // console.assert(beta(0.6).variance === 0, 'Etc.');

    // // Tests for the median of the beta distribution.
    // console.assert(beta(0.3).median === 0, 'Etc.');
    // console.assert(beta(0.5).median === 0, 'Etc.');
    // console.assert(beta(0.8).median === 0, 'Etc.');

    // // Tests for the probability density function of the beta distribution.
    // console.assert(beta(0.2).pdf(-1) === 0, 'Etc.');
    // console.assert(beta(0.3).pdf(0) === 0, 'Etc.');

    // // Tests for the cumulative distribution function of the beta distribution.
    // console.assert(beta(0.35).cdf(0) === 0, 'Etc.');
    // console.assert(beta(0.22).cdf(0) === 0, 'Etc.');
}